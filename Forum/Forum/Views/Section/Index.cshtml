@model IEnumerable<Forum.Models.Section>

<div class="conteiner p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Section List</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="Section" asp-action="Create" class="btn btn-success"><i class="bi bi-plus-square"></i> Create new Section</a>
        </div>
    </div>

    <br /><br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <tread>
                <tr>
                    <th>
                        Section Name
                    </th>
                    <th>
                        Description
                    </th>
                    <th>
                        Create Time
                    </th>
                    <th>
                        Last Change Time
                    </th>
                    <th></th>
                </tr>
            </tread>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td style="width: 20%;">@obj.Name</td>
                        <td style="width: 30%;">@obj.Description</td>
                        <td style="width: 20%;">@obj.CreateTime</td>
                        <td style="width: 20%;">@obj.LastChangeTime</td>
                        <td style="width: 10%;" class="text-center">
                            <div class="w-55 btn-group" role="group">
                                <a asp-controller="Section" asp-route-Id="@obj.Id" asp-action="Update" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i>
                                </a>
                                <a asp-controller="Section" asp-route-Id="@obj.Id" asp-action="Delete" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash3"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No Section exists.</p>
    }

</div>

